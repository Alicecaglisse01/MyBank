<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Utilisateurs</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #e0e7ff; /* Couleur de fond douce */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: #ffffff;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Augmentation de l'ombre */
            max-width: 800px;
            width: 100%;
            box-sizing: border-box;
            transition: transform 0.3s ease; /* Ajout d'une transition pour l'effet de survol */
        }
        .container:hover {
            transform: translateY(-10px); /* Effet de survol */
        }
        h1 {
            color: #1f2937; /* Couleur de texte plus foncée */
            font-size: 2.5rem; /* Agrandissement de la police */
            text-align: center;
            margin-bottom: 1.5rem;
        }
        .user-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .user-item {
            display: flex;
            justify-content: space-between;
            background-color: #f3f4f6; /* Couleur de fond plus claire */
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1rem;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s ease; /* Ajout d'une transition pour l'effet de survol */
        }
        .user-item:hover {
            background-color: #e5e7eb; /* Couleur de survol */
            transform: translateY(-5px); /* Effet de survol */
        }
        .user-name {
            font-weight: bold;
            color: #1f2937; /* Couleur de texte */
        }
        .user-email {
            color: #4b5563; /* Couleur de texte */
        }
        .user-creation-ts {
            color: #6b7280; /* Couleur de texte */
        }
        .back-button {
            margin-top: 1rem;
            display: flex;
            justify-content: center;
        }
        .back-button button {
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            color: #ffffff;
            background-color: #4CAF50;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease; /* Ajout d'une transition pour l'effet de survol */
        }
        .back-button button:hover {
            background-color: #45a049;
            transform: translateY(-5px); /* Effet de survol */
        }
    </style>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            document.querySelectorAll('.user-item').forEach(function(item) {
                item.addEventListener('click', function() {
                    window.location.href = '/views/' + item.dataset.userid;
                });
            });
        });
    </script>
</head>
<body>
    <div class="container">
        <h1>Liste des Utilisateurs</h1>
        <ul class="user-list">
            <% users.forEach(function(user) { %>
                <li class="user-item" data-userid="<%= user.ID %>">
                    <div>
                        <div class="user-name"><%= user.NAME %></div>
                        <div class="user-email"><%= user.EMAIL %></div>
                    </div>
                    <div class="user-creation-ts"><%= new Date(user.CREATION_TS).toLocaleString() %></div>
                </li>
            <% }); %>
        </ul>
        <div class="back-button">
            <button onclick="window.location.href='/'">Accueil</button>
        </div>
    </div>
</body>
</html>
